{
  "name": "cascata",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Roguelike engine-builder de reações em cadeia. Browser, zero assets externos.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "harness": "tsx src/sim/harness.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
